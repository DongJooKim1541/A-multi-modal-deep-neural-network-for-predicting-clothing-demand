# CLAUDE.md

이 저장소에서 작업할 때 지킬 관례. 형제 저장소들과 공유하는 규칙이다.

## 커밋

- 커밋 메시지는 **한국어**로 쓴다.
- prefix: `feat:` `fix:` `refactor:` `docs:` `chore:`
- 제목 뒤 접미: 없음.
- 기본 브랜치(main)에서 바로 커밋하지 말고 작업 브랜치를 만든 뒤 머지한다.

## 문서

### 목차

- README의 목차 제목은 `## 📋 Quick Navigation` 이다. 목차 제목의 언어는 **절 제목의 언어를 따른다** (이 저장소의 절 제목은 영어).
- 배치: `---` + 빈 줄 + 목차 제목 + 빈 줄 + 목록 + 빈 줄 + `---` + 빈 줄 + 첫 절
- **모든 `##` 절을 빠짐없이** 목차에 넣는다. 절을 추가·삭제·개명하면 같은 커밋에서 목차도 고친다.
- 라벨은 후행 괄호를 뗀다. 예: 제목 `## Quick Start (5 commands)` → 라벨 `Quick Start`, 앵커 `#quick-start-5-commands`

### 앵커 링크

GitHub의 slug 규칙은 소문자화 → 기호 제거 → 공백을 하이픈으로 치환이며, **연속 하이픈을 합치지 않는다.**

- `## Configuration & Customization` → `#configuration--customization` (하이픈 2개)
- `## 프로젝트 구조` → `#프로젝트-구조` (한글은 그대로 유지)

링크를 검사하는 스크립트를 짤 때 `\s+` → `-` 로 합치면 멀쩡한 링크를 오탐한다.

### 날짜 표기 금지

`Last Updated`, `Document Version: <날짜>`, `마지막 수정`, `프로젝트 완료 날짜` 같은 **수동 갱신 날짜를 문서에 두지 않는다.** 손으로 고쳐야 해서 실제 최종 수정 시점과 어긋나고, 갱신 이력은 git이 정확히 갖고 있어 중복이다. README와 `docs/` 양쪽 모두 해당한다. 날짜가 아닌 푸터 항목(`Status`, `Version: 1.0`, 커버리지 수치)은 남겨도 된다.

## 코드를 고치면 문서까지 한 묶음

코드·파일명·디렉토리 구조를 바꿨으면 같은 작업 안에서 README와 `docs/`(SDD.md, TC.md)를 실제 상태와 대조한다. 지금까지 생긴 링크 깨짐은 전부 "문서 일부만 고치고 나머지를 대조하지 않아서" 발생했다.

- 제목을 바꾸고 목차 앵커를 안 고친 경우
- 파일을 git에서 뺐는데 그 파일을 가리키는 링크가 남은 경우
- 구조도 트리의 파일명이 실제 파일명과 다른 경우
